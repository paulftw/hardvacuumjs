define([], function() {

    var FindPath = function(x1, y1, x2, y2, terrain) {
        var grid = new PF.Grid(terrain.width + 2, terrain.height + 2);
        for (var x = 0; x < 
    };

    return {
    };
});
